interface:
  display_name: "Consolidate Test Suites"
  short_description: "Place durable tests and clean up temporary probes."
  default_prompt: "Use $consolidate-test-suites to place durable coverage in its canonical suite and promote, merge, or remove temporary test probes."

policy:
  allow_implicit_invocation: true
