# HOL Guard

Protect a supported local coding-agent harness with HOL Guard before state-changing commands and tool calls.

## Install

```bash
npx skills add instructa/agent-skills --skill hol-guard
```

Use `-g` for a global install. See [SKILL.md](SKILL.md) for the fail-closed setup, dry-run, launch, and verification workflow.
